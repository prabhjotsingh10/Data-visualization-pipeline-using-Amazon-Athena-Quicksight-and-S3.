CREATE EXTERNAL TABLE billing_sample_data (
  date string,
  service string,
  resource string,
  cost double
)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.OpenCSVSerde'
LOCATION 's3://enterprisecosreportbucket/Static-data-sample-billing-folder-not-CUR/'
TBLPROPERTIES (
  "skip.header.line.count"="1", **ignoring the first line of the table column to avoid errors**
  "use.null.for.invalid.data"="true" **Ignoring null data in case if it's encountered by Athena while running the queries**
);